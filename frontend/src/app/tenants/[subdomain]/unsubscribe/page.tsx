'use client';

import React from 'react';
import UnsubscribeContent from '@/components/addons/newsletter-campaigner/UnsubscribeContent';

export default function TenantUnsubscribePage() {
  return (
    <div className="min-h-screen bg-[#020403] flex items-center justify-center p-6">
      <UnsubscribeContent />
    </div>
  );
}
